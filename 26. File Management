#include <stdio.h>

int main()
{
    FILE *fp;
    char data[100];

    fp=fopen("file.txt","w");

    fprintf(fp,"Hello World\n");
    fclose(fp);

    fp=fopen("file.txt","r");

    while(fgets(data,100,fp))
        printf("%s",data);

    fclose(fp);

    return 0;
}
